let word = prompt();
let vowel = [a, e, i, o, u, A, E, I, O, U];
if (word.include(vowel));{
    alert('სიტყვა იწყება ხმოვანით, რომელიც არის '(word.charAt(0)))
}
else {
    alert('სიტყვა არ იწყება ხმოვანით, რადგან ის არის'(word.charAt(0)));
}